<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>게임 마켓 - 로그인</title>
  <link rel="stylesheet" href="/css/common/login.css" />
</head>
<body>
  <div class="login-container">
    <h1>게임 마켓 로그인</h1>
    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>
    <form action="/auth/login" method="post">
      <input name="id" type="text" placeholder="아이디" value="<%= typeof id !== 'undefined' ? id : '' %>" required />
      <input name="password" type="password" placeholder="비밀번호" required />
      <button type="submit">로그인</button>
    </form>
  <a href="/users/sign_in" class="link-btn">회원가입</a>

    <div class="divider"><span>또는</span></div>
    <div class="role-buttons">
      <a href="/users/game_list" class="user-btn">사용자 페이지</a>
      <a href="/admin/game_list" class="admin-btn">관리자 페이지</a>
    </div>
  </div>
</body>
</html>
